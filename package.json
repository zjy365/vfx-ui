{
  "name": "vfx-ui",
  "private": true,
  "version": "0.1.0",
  "description": "Shader-native visual effect components for React, rendered via WebGPU (vgpu).",
  "license": "MIT",
  "packageManager": "pnpm@8.15.9",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "dev:docs": "pnpm --filter @vfx-ui/docs dev",
    "dev:playground": "pnpm --filter @vfx-ui/playground dev"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
